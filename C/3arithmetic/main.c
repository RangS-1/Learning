#include <stdio.h>
#include <stdbool.h>

int main(){
    int x = 16;
    int y = 5;
    int z = 0;

    //z = x + y;
    //z = x - y;
    //z = x * y;
    //z = x / y;
    //z = x % y;
    //x += 4;
    printf("%d", x);

    return 0;
}